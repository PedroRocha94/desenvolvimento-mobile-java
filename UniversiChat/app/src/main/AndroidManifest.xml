<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ufc.UniversiChat">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_VOICEMAIL" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskManagerProjetoFinal"
        android:usesCleartextTraffic="true">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBuX7nU0aQ9GOfi8YZMiTC-1rNQ0wl2vTY" />

        <activity
            android:name="com.ufc.UniversiChat.activity.MapsActivity"
            android:exported="false"
            android:label="Localização do Usuário" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name="com.ufc.UniversiChat.activity.InformacoesActivity"
            android:exported="false" />
        <activity
            android:name="com.ufc.UniversiChat.activity.CadastroTarefaActivity"
            android:exported="false"
            android:parentActivityName="com.ufc.UniversiChat.activity.TarefaActivity" />
        <activity
            android:name="com.ufc.UniversiChat.activity.TarefaActivity"
            android:exported="false"
            android:parentActivityName="com.ufc.UniversiChat.activity.MainActivity" />
        <activity
            android:name="com.ufc.UniversiChat.activity.ChatActivity"
            android:exported="false"
            android:parentActivityName="com.ufc.UniversiChat.activity.MainActivity" />
        <activity
            android:name="com.ufc.UniversiChat.activity.ConfiguracoesActivity"
            android:exported="false"
            android:parentActivityName="com.ufc.UniversiChat.activity.MainActivity" />
        <activity
            android:name="com.ufc.UniversiChat.activity.CadastroActivity"
            android:exported="false" />
        <activity
            android:name="com.ufc.UniversiChat.activity.MainActivity"
            android:exported="false" />
        <activity
            android:name="com.ufc.UniversiChat.activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>